You are an expert algorithm researcher analyzing a research paper. Your task is to extract the key algorithm information from the paper.

Paper Title: {title}

Paper Abstract:
{abstract}

Paper Content:
{paper_content}

Please analyze the paper and extract the following information:

1. **Algorithm name and type**:
   - What is the name of the algorithm/method?
   - What type of algorithm is it? (classification, regression, generation, optimization, clustering, etc.)

2. **Core logic and main steps**:
   - What are the main components of the algorithm?
   - What is the key mathematical formulation?
   - How does the algorithm work at a high level?

3. **Pseudocode or algorithm descriptions**:
   - Extract any pseudocode provided in the paper
   - Describe the algorithm steps in detail

4. **Hyperparameters and their default values**:
   - List all hyperparameters mentioned
   - Note their default values or typical ranges
   - Explain what each parameter controls

5. **Dataset requirements**:
   - What type of data does the algorithm work with?
   - Are there specific dataset requirements (image size, features, etc.)?
   - Are example datasets mentioned (MNIST, CIFAR, etc.)?

6. **Required frameworks or libraries**:
   - Which ML frameworks are used (PyTorch, TensorFlow, JAX, etc.)?
   - What other libraries are needed (NumPy, SciPy, sklearn, etc.)?
   - Any specific version requirements mentioned?

7. **Computational requirements**:
   - Does it require GPU?
   - What are the memory requirements?
   - Any specific hardware mentioned?

8. **Data flow and architecture**:
   - Describe the overall architecture
   - How does data flow through the system?
   - What are the key modules/components?

Provide your response as a structured JSON with these fields.
IMPORTANT: Return ONLY the JSON object, no markdown formatting, no code blocks, no explanations.

JSON Format (return only this structure):
{{
  "algorithm_name": "string",
  "algorithm_type": "string",
  "core_logic": "string",
  "pseudocode": "string",
  "hyperparameters": {{
    "param_name": "value or range",
    "param_name2": "value or range"
  }},
  "requirements": {{
    "dataset": "string",
    "frameworks": ["framework1", "framework2"],
    "compute": "string"
  }},
  "data_flow": "string"
}}

Ensure your JSON is valid and can be parsed with standard JSON parsers. Return ONLY the JSON.
